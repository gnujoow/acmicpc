#include <stdio.h>
#include <string.h>

int main(){
	char cmd[50][50];
	int N;

	scanf("%d",&N);
	for(int i = 0 ; i < N ; i++){
		scanf("%s",cmd[i]);
	}

	int len = strlen(cmd[0]);

	
	for(int j = 0 ; j < len ; j++){
		for(int i = 0 ; i < N ; i++){

		}
	}

	return 0;
}